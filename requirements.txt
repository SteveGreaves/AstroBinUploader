# Runtime dependencies actually imported by this project.
#
# B7 in REMEDIATION_PLAN.md: this file used to be a raw `pip freeze` of an
# entire development environment (matplotlib, jupyter, ipython, requests,
# PyYAML, debugpy, beautifulsoup4, and more -- 50+ packages, most never
# imported by any module here). Trimmed to what the code actually uses,
# confirmed by grepping every import across the codebase; everything else
# needed is Python standard library (argparse, concurrent.futures,
# dataclasses, datetime, enum, logging, os, re, struct, sys, typing, xml).
#
# GeocodeStep's *distance* maths is a hand-rolled vectorized haversine, not
# geopy, and that has not changed (the A3/A4 fix; see the module docstring in
# engine/steps/geocode.py for why a per-row geopy call was the wrong shape).
astropy==6.1.3
pandas==2.2.3
numpy==2.1.1
configobj==5.0.8

# Imported lazily, only at the moment a coordinate that [sites] does not already
# know needs resolving (engine/sites.py). `pip install -r requirements.txt`
# fetches both, as it always has. If either is missing the lookup degrades
# instead of failing: the site name and its Bortle/SQM come from [defaults],
# exactly as in v2.0.0-v2.1.3, and the run finishes normally. Restored in v2.2.0
# along with the lookups themselves.
geopy==2.4.1
requests==2.32.3
